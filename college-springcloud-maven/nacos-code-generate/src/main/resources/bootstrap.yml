spring:
  application:
    name: chenfan-cloud-code-generate
  cloud:
    nacos:
      server-addr: ${nacos.server:10.228.81.30}:${nacos.port:3848}
      discovery:
        namespace: ${profiles_active:dev}
        group: ${group:DEV}_GROUP
  redis:
    database: 4
    host: 10.228.81.233
    port: 6379
    timeout: 100000